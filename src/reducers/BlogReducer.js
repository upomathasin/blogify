import { actions } from "../actions";

const initialBlog={
    blogs:[],

}

const BlogReducer=(state,action)=>{


    switch(action.type){
        case action.type== actions.blog.BLOG_FETCHING:{
            return [

            ]
                
            
        }
     
    }
}